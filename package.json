{
  "name": "vue-events-bulletin-board",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/app.js -o bundle.js -t [ babelify ]",
    "watch": "watchify src/app.js -o bundle.js -t [ babelify ] -v"
  },
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^3.3.6",
    "vue": "^1.0.14",
    "vue-resource": "^0.6.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "http-server": "^0.8.5",
    "watchify": "^3.7.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
